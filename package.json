{
  "name": "yassine-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack junior portfolio built with React, NestJS, and PostgreSQL.",
  "scripts": {
    "dev": "concurrently \"npm run start:dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run start:dev --prefix server",
    "build": "npm run build --prefix server && npm run build --prefix client",
    "lint": "npm run lint --prefix server && npm run lint --prefix client",
    "db:up": "docker compose up -d postgres",
    "db:down": "docker compose down",
    "db:push": "npm run prisma:push --prefix server",
    "db:seed": "npm run prisma:seed --prefix server"
  },
  "keywords": [
    "portfolio",
    "react",
    "nestjs",
    "postgres"
  ],
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
